# Фінальний чеклист для production deploy

## ✅ Виправлені файли (готові до завантаження)

### Критичні файли:
1. **`.htaccess`** - ОБОВ'ЯЗКОВО! Без нього нічого не працює
2. **`templates/partials/header.php`** - виправлено `$settings` → `$blogSettings`
3. **`src/Controllers/Admin/SettingsController.php`** - додано підготовку змінних
4. **`storage/database.sql`** - додано `parent_id` в таблицю comments

### Додаткові файли:
5. `templates/pages/about.php` - виправлено backslash
6. `assets/css/theme.css` - рефакторинг CSS
7. `assets/css/base.css` - додано CSS змінні

## 🗄️ SQL міграція для існуючих БД

Якщо БД вже створена (без переінсталяції), виконай:

```sql
ALTER TABLE `comments` 
ADD COLUMN `parent_id` int(11) UNSIGNED NULL AFTER `post_id`,
ADD KEY `parent_id` (`parent_id`),
ADD CONSTRAINT `fk_comments_parent` FOREIGN KEY (`parent_id`) 
  REFERENCES `comments` (`id`) ON DELETE CASCADE;
```

## 🆕 Для нової інсталяції

Просто запусти `/install.php` - все буде створено автоматично з правильною структурою БД.

## 🧹 Після deploy

1. Очисти кеш: `rm -rf storage/cache/*`
2. Видали `test.php` (якщо завантажував)
3. Перевір права: `chmod 755 storage/cache`

## ✅ Перевірка

- [ ] Головна сторінка `/` працює
- [ ] `/about` працює
- [ ] `/archive` працює
- [ ] `/admin/settings` працює (для admin)
- [ ] Коментарі можна додавати
- [ ] CSS завантажується правильно
